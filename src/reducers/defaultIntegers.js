const initialState = {
    m: 10, // number of rows
    n: 5,  // number of columns
    x: 10   // number of cells whcih should be highlighted
}
export default function(state = initialState) {
    return state;
}