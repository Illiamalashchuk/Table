const initialState = {
    m: 50, // number of rows
    n: 10,  // number of columns
    x: 15   // number of cells whcih should be highlighted
}
export default function(state = initialState) {
    return state;
}