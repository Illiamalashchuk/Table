const initialState = {
  m: 50, // number of rows
  n: 10, // number of columns
  x: 30, // number of cells which should be highlighted
};

export default function (state = initialState) {
  return state;
}
